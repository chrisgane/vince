const withPlugins = require("next-compose-plugins");

const config = {
    // images: {
    //     domains: ["stupefied-antonelli.136-244-69-22.plesk.page"],
    // },
};

module.exports = withPlugins([], config);
